# Test configuration
quarkus.datasource.db-kind=h2
quarkus.datasource.jdbc.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
quarkus.hibernate-orm.database.generation=drop-and-create
quarkus.hibernate-orm.log.sql=false

# JWT config for tests
mp.jwt.verify.publickey.location=META-INF/resources/publicKey.pem
smallrye.jwt.sign.key.location=META-INF/resources/privateKey.pem

# Google API mock config for tests
google.cse.id=test-cse-id
google.api.key=test-api-key

# Disable MCP Server for tests
quarkus.mcp.server.stdio.enabled=false
quarkus.mcp.server.sse.enabled=false
